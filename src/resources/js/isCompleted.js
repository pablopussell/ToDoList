function isCompleted(){
	if (document.getElementById('isCompleted').checked) {
		document.getElementsByClassName("view").getElementsByTagName("span").style.textDecoration="line-through";
	} else {

	}};